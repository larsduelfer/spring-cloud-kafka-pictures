micronaut:
  application:
    name: like-service
  config-client:
    enabled: true

spring:
  cloud:
    config:
      enabled: true
      uri: http://config:secret@localhost:10000/like-service/local